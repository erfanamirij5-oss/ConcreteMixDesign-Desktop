# G02A Exit Status

Current status: PARTIAL — provenance and fail-closed verification foundation complete; numerical certification intentionally blocked.

Closed:
- authoritative ACI document identity and public scope evidence recorded;
- executable normal-weight rule inventory established;
- machine-readable verification states established;
- existing strength–w/cm extrapolation refusal locked by regression tests;
- independent absolute-volume arithmetic golden case added;
- no existing numerical lookup silently promoted to VERIFIED.

Open before G02A can be CLOSED:
- authorized exact-edition evidence for numerical lookup relationships;
- exact-cell/node and interpolation golden tests for those relationships;
- resolution of placeholder air-entrained default;
- explicit fail-closed policy for slump outside implemented table domain;
- explicit policy for non-tabulated NMSA rather than implicit certification of nearest-value snapping;
- rule-key traceability in production Standard Profile output/report envelope.
